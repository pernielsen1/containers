# todo — 2026-08-08

Status snapshot / handoff for continuing the debug-tracing initiative (`briefs/debug_trace_master.md`).

## Done this round

Ported the Round 5 debug-tracing fixes from `router_py` (all 5 phases already complete there) to
`router_java` and `router_cpp`, in the requested priority order:

| | router_java | router_cpp |
|---|---|---|
| (1) Session-teardown pending-drop fix | done, 5/5 tests pass | done, verified via new test suite + Docker |
| (2) crypto_host `router_stan` correlation | done, verified live (jar) | done, verified live (binary) |
| (3) Monitor UI | **deliberately not done** | **deliberately not done** |

Both languages' `Dispatcher`/`CryptoClient` are near-1:1 ports of each other, so the fixes
transferred almost mechanically once verified in one.

Bonus: `router_cpp` had **zero** Dispatcher-level tests before this (unlike `router_py`/
`router_java`) — built `router_cpp/test/dispatcher_test.cpp` from scratch, porting all 5 scenarios
(TTL expiry, queue-full blocking, STAN collision, the new drain-and-stop fix, purge). C++ has no
reflection, so private-member test access needed `friend class DispatcherTestAccess` /
`friend class DownstreamConnectionTestFactory`. All 91 assertions / 29 test cases pass locally and
the Docker image rebuilds clean.

Everything is committed as `6cfbf7d7 "more resilience"`.

## Explicitly deferred

- **Monitor UI for router_java/router_cpp** — holding until the redesign gets worked through, not
  assuming `router_py`'s hand-built-per-language modal approach is the template to copy.

## Next up

- **router_java crypto-client warmup latency under real concurrent load** (found 2026-08-16
  chasing a `router_java` stress-run correlation failure — `sent=489;received=0;errors=489`,
  `upstream_host` logging `no pending request for STAN N` for every request). Root cause of the
  0-received symptom itself is fixed (`upstream_host/main.py`'s `_run_with_warmup` was clearing
  `pending` on a fixed 2s timer instead of waiting for in-flight warmup responses to drain - now
  polls until `pending` is actually empty, capped at `max(warmup_s, 30s)`). But the thing that
  triggered it is still open: `CryptoClient.java`'s per-thread warmup (`Dispatcher.java` - 16
  threads, each building its own `HttpClient`/`SSLContext` and doing a real mTLS call to
  `crypto_host`, all serialized one-at-a-time through `warmupGate`) took *far* longer to connect
  under real dispatcher startup than an isolated single-threaded probe against the same live
  `crypto_host` (which succeeded in 1.5s) - warmup calls were hitting the full 5-20s timeout
  repeatedly. Not yet root-caused: candidates are CPU/memory contention on this host (only 2.8GB
  RAM, 8 cores, shared with whatever else is running) starving the 16 concurrent thread
  startups, or something specific to serialized-but-back-to-back TLS handshake setup. Re-test
  once this host is dedicated to a soak run (nothing else competing for CPU/RAM) to see whether
  the slowness reproduces in isolation - if it doesn't, this was host contention, not a code bug.

- **Hunt down the "Bad file descriptor" issue** in `router_py`'s `router/downstream.py` reconnect
  path (`downstream connection lost: recv failed: [Errno 9] Bad file descriptor`). Recurred once
  during a normally-paced `test_resilience.py` run, not just under tight test-loop cycling — raises
  confidence it's a real race, not purely a test artifact. Not yet investigated.
- Clean up leftover "xv6" naming in `router_java` (`com.xv6.*` package) and `router_cpp`
  (`xv6::router`/`xv6::shared` namespace, `xv6_router`/`xv6_shared`/`xv6_tests` CMake targets) —
  cosmetic legacy from before the 2026-08-01 rename, not functional. Plan as its own deliberate
  pass (touches every file in both trees), not a drive-by edit.
- When porting resilience testing to `router_java`/`router_cpp`: check whether their
  session-teardown path has the same silent-pending-drop gap `router_py` had (near-certain, same
  as the fix just ported) and add the equivalent `test_resilience.py`-style live scenario suite.
