create a development container company_id_container
the container should have mysql, python and claude installed.
the user for mysql is in environment variable PN_MYSQL_USER the password in PN_MYSQL_PASSWORD
a python script test_company_id.py shall:
create a database called db
create a table company_ids with three character fields  CNTRY char(2), ID_TYPE char (3), ID char(50)
load the contents of company_ids.csv into the table.
create a stored procedure called validate_company_id taking CNTRY. ID_TYPE and ID as input which returns 1 if CNTRY is 'NO' or 'SE'
list the contents of a select from the table return all fields plus the result of callind validate_company_id stored procedure as field validation_result

sg docker -c "docker build -t company_id_container            
  /home/per-nielsen/containers/claude_exp/company_id_sql/"
sudo usermod -aG docker per-nielsen && newgrp dockersudo usermod -aG docker per-nielsen && newgrp docker


company_id_sql-company_id_container:latest

sudo docker run -i -t company_id_sql-company_id_container:latest

the snippets/company_identifiers.py contains a class that can validate a 
company id and a vat id.
focus first on the validate_COMPANY_ID function taking a string s i.e. the company_id and a country code as input.
the csv we started with has a CNTRY which is the country and an ID which is the company ID
in test_company_id we loaded the csv into a table and created a stored procdure.
Now target is to create a stored procedure validate_company_id taking the CNTRY and ID as input and returning 
1 of OK and 0 if not. 
The validation should be based on the validate_COMPANY_ID in company_identifiers.py

Next was to do it on vat - id - forgot to store it.

snippets_copy/tests/test_company_identifiers.py contains test cases for validate_vat_id as well
identified as the calls to self.m_obj.validate_VAT_ID_bool which returns True or False.
extract the test cases and store in company_ids.csv with TYPE=VAT. CNTRY is the first parameter passed to 
validate_VAT_ID_bool and ID is the second parameter. EXPECTED is derived from True or False expection to the AssertEquals

Now update test_company_id.py to have two passes the first reads rows with TYPE='CID' 
and uses validate_company_id. 
The second reads rows with TYPE='VAT' and uses validate_vat_id

the validation of company_ID for Germany (DE) seems to have missed the check of 
the string after versus the XJustiz dictionary. 
The XJustis dictionary could be converted into a table in the database xjustiz.

Doing good even xjustiz for Germany is correct - but not all cases for Romania passes correct when running 
test_company_id.py  In validate_company_id.sql the cases /J/AB/12345/1999 and JAB/12345/1999 both fails 
but are correct in the original python code found in /snippets_copy/company_identifiers.py function validate_romania
Two failing cases have 4 forwards slashes respectively 2 forward slashes. The current code only handles the case with 3 slashes.
Both 4 and 2 can be correct and should be handled like in the validate_romania funcion in company_identifiers.py

create a new sql stored procedure named get_xjustiz_code inside the validate_company_id.sql 
I should for a german CID (CNTRY='DE') return the court_code from xjustiz table. if not found return empty string ''

now update test_company_id.py to accept not only CID and VAT as parameter but also XJUSTIZ - if parameter is XJUSTIZ then for all 
rows in company_ids.csv where CNTRY='DE' then find the XJUSTIZ code using the get_xjustis_code stored procedure.
print a list with CNTRY, CID and XJUSTIZ

ToDo: EU and CH

The validate_vat_id.sql is wrong for CNTRY='CH' and fails in case CH123.456.788 - 
it assumes that the string starts with CHE which is should not do for VAT-ID. 
In original snippets_copy/company_identifiers.py that VAT for CH points to standard VAT id -calculation 
CH followed by 9 digits the last digit is modulus 11 calculated 
with the weights "weights": [ 5, 4, 3, 2, 7, 6, 5, 4, 1 ]}, 

OK last strange case for BE it can start with EU instead of BE. This is reflected in original validate_vat_std routing in python 
basically saying if the string starts with EU then basically override the country code and use the rules for EU as country code
