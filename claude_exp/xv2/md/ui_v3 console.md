# monitor display log
each actor will be logging information based on the log level set.
in the monitor it should be possible to
1: change log level DEBUG, INFO, WARNING, ERROR - this would be a new command for all actors.
2: See the log in a window - where it is possible to scroll in the log and even export to text file.

