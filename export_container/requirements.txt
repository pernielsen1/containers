# AnaCredit container dependencies
# stdlib-only for validators — only external deps are PDF/Excel parsing and Claude API

# Claude API client
anthropic>=0.40.0

# PDF text extraction (extract_attributes.py)
pdfplumber>=0.11.0

# Excel codelist reading
openpyxl>=3.1.0
