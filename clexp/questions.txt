The bista skill is based on two PDF's please list two names of the PDF's
The "banking statistics guidelines" list items on a more detailed level thatn bista_items.csv. 
Example the Item 010 Cash in hand and Item 011 Domestic legal tender and Item 020 Balances with central banks  are not listed in the bista_items.csv and thus not in the other mapping files. 
update the skill to include all items from the "bank statistics guideline"

Internal tip: explorer.exe . shows the current directory in windows
sort the bista.csv file based on item - ascending 

please update mapping.csv and mapping.ifrs to reflect the changes   

The bista in the end needs to be submitted as an xml - 
this document: https://www.bundesbank.de/resource/blob/612316/4271d94656d562627c49d6ad672d6d02/472B63F073F071307366337C94F8C870/engl-guide-xmw-bsm-12-2014-data.pdf
describes how. 
download it to docs 
read it and make a python script converting the output csv file from bista_mapper.py to an XML file.
The XML file will also need information of who is the "melder" i.e. the reporting organization
read that fixed information from a json file

even though you cannot verify the execution of the python script 
it is by design - 

make a bash script test_bista.sh that first runs the bista_mapper.py in with the ifrs mapping scheme and input file the sample_gl.csv
then the output from this script as input to to the bista_to_xml.py

the bista_items.csv has to long description to work as input to pandas
make one version of bista_items.csv called bista_items_full.csv a copy of 
current bista_items.csv and one version bista_items_short.csv without the description field

update bista_mapper.py to use the bista_items_short.csv

OK that was not a smart move - please revert it
OK ignore - try to run the bista_mapper.py it generates and error - fix the error

3 items (HV21_250, HV21_320, HV21_325) each have 2 conflicting rows. 
  Which row should we keep — the first or second occurrence? Or do you 
  want to decide per item?   fi
  keep first1

  3 items (HV21_250, HV21_320, HV21_325) each have 2 conflicting rows. 
  Which row should we keep — the first or second occurrence? Or do you 
  want to decide per item?   

  time for a deploy script - make deploy.sh which copies everything in the           
experiments folder to existing folder ../containers/experiments - let me review and  
then we try  

time to make the test suite - add a verification script that tests that the produced xml is validated according to the XSD 
All three stages pass. Summary of what the    
  verifier checks:                              
                                                
  Structural checks (always run):               
  - Root tag is LIEFERUNG-BISTA in the correct  
  Bundesbank namespace                          
  - stufe ∈ {Test, Produktion}, bereich =       
  Statistik                                     
  - erstellzeit timestamps in                   
  YYYY-MM-DDTHH:MM:SS format                    
  - MELDUNG present with MELDER (BLZ + NAME),
  MELDETERMIN in YYYY-MM format                 
  - LAND is a 2-letter ISO code
  - Each FORMULAR: name ∈ {HV}, modus ∈ {Normal,
   Korrektur, Storno}
  - Each FELD pos matches Z###S## pattern
  - No duplicate positions
  - All amounts are non-zero integers

  XSD validation (runs if BbkXmwBsm.xsd is
  present):
  - Drop the file from Bundesbank ExtraNet next
  to the XML and it will automatically be used

  The one warning is expected — XSD file needs
  to come from Bundesbank ExtraNet.

✻ Cogitated for 2m 47s

the bista_item.csv has errors it contains items not existing.
items shall only be included if they appear as a header in bold.
example of error is HV11_021 which does not exist in the pdf.
correct the bista_items.csv


Time for a new skill AnaCredit 
Info:
I have made a new directory "AnaCredit" and downloaded relevant documents to docs directory.
One PDF "ana-credit guidelines" and one Excel "anacredit-codelist"
which is the just used for referencing the values. The PDF - will often refer to this with a "URL" -link ignore it and just use this Excel as the reference data.

Guidance:
Start by understanding the PDF and interpretate the Data Attributes for each Data Set.
Each item will be in header with the test "Data attribute" 
In chapter four the data set "counter party reference data" is explained 
Example for one data item from chapter 4 is "Data attribute: Type of counterparty identifier"

In chapter 5 a number of data sets for credit data are explained starting with 
5.1 Instrument data set - where the first item "Data Attribute is Contract Identifier"

Goal: Understand the PDF "ana-credit guidelines" - and make one basic csv with the columns:
data set
Data Attribute
Description which is all text from the data attribute until the next data attribute or next data set or - "end of chapter"
