  ${SERVICE_NAME}:
    image: fastapi/pn_app
    ${BUILD_COMMAND}
    container_name: "${SERVICE_NAME}"
    volumes:
      - ./app:/app
    ports:
      - ${PORT}:${PORT}
    entrypoint:
      - fastapi
      - run
      - main.py
      - --port 
      - "$PORT"
    